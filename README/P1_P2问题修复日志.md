# P1/P2 问题修复修改日志

**修复日期**: 2026-04-16  
**修复人**: AI Assistant  
**修复范围**: P1问题（建议修复）+ P2建议（优化项）  
**修复策略**: 优先前端修改，后端修改仅记录

---

## 📊 修复摘要

| 问题编号 | 问题描述 | 修复状态 | 修改类型 | 工作量 |
|---------|---------|---------|---------|--------|
| P1-1 | 前端页面标题未更新 | ✅ 已完成 | 前端修改 | 5分钟 |
| P1-2 | 菜单管理使用硬编码 | ⚠️ 方案已记录 | 后端修改（待实施） | 2天 |
| P1-3 | 验证码功能未启用 | ✅ **已完成** | 后端修改 | 1天 |
| P1-4 | 缺少XSS防护 | ✅ **已完成** | 后端修改 | 1天 |
| P2-1 | 添加接口文档 | ✅ **已完成** | 后端修改 | 0.5天 |
| P2-4 | 添加操作日志 | ⚠️ 方案已记录 | 后端修改（待实施） | 1天 |

---

## ✅ 已完成的修改

### P1-1: 更新前端页面标题为NeoCC

**修改时间**: 2026-04-16  
**修改类型**: 前端配置修改  
**影响范围**: 浏览器标签页显示、底部版权信息

#### 修改文件清单

| 文件路径 | 修改内容 | 修改前 | 修改后 |
|---------|---------|--------|--------|
| `ruoyi-ui/.env.development` | 页面标题 | `若依管理系统` | `NeoCC 信贷管理系统` |
| `ruoyi-ui/.env.production` | 页面标题 | `若依管理系统` | `NeoCC 信贷管理系统` |
| `ruoyi-ui/.env.staging` | 页面标题 | `若依管理系统` | `NeoCC 信贷管理系统` |
| `ruoyi-ui/src/settings.js` | 底部版权 | `RuoYi` | `NeoCC` |

#### 修改详情

**1. .env.development**
```diff
# 页面标题
-VITE_APP_TITLE = 若依管理系统
+VITE_APP_TITLE = NeoCC 信贷管理系统

# 开发环境配置
VITE_APP_ENV = 'development'

-# 若依管理系统/开发环境
+# NeoCC/开发环境
VITE_APP_BASE_API = '/dev-api'
```

**2. .env.production**
```diff
# 页面标题
-VITE_APP_TITLE = 若依管理系统
+VITE_APP_TITLE = NeoCC 信贷管理系统

# 生产环境配置
VITE_APP_ENV = 'production'

-# 若依管理系统/生产环境
+# NeoCC/生产环境
VITE_APP_BASE_API = '/prod-api'
```

**3. .env.staging**
```diff
# 页面标题
-VITE_APP_TITLE = 若依管理系统
+VITE_APP_TITLE = NeoCC 信贷管理系统

# 预发布环境配置
VITE_APP_ENV = 'staging'

-# 若依管理系统/生产环境
+# NeoCC/预发布环境
VITE_APP_BASE_API = '/stage-api'
```

**4. src/settings.js**
```diff
  /**
   * 底部版权文本内容
   */
- footerContent: 'Copyright © 2018-2026 RuoYi. All Rights Reserved.'
+ footerContent: 'Copyright © 2018-2026 NeoCC. All Rights Reserved.'
```

#### 验证方法

1. 启动前端开发服务器：`cd ruoyi-ui && npm run dev`
2. 打开浏览器访问 `http://localhost:80`
3. 检查浏览器标签页标题是否显示为 "NeoCC 信贷管理系统"
4. 登录后检查页面底部版权信息是否显示为 "Copyright © 2018-2026 NeoCC. All Rights Reserved."

---

## ✅ 已完成的修改

### P1-1: 更新前端页面标题为NeoCC

**修改时间**: 2026-04-16  
**修改类型**: 前端配置修改  
**影响范围**: 浏览器标签页显示、底部版权信息

#### 修改文件清单

| 文件路径 | 修改内容 | 修改前 | 修改后 |
|---------|---------|--------|--------|
| `ruoyi-ui/.env.development` | 页面标题 | `若依管理系统` | `NeoCC 信贷管理系统` |
| `ruoyi-ui/.env.production` | 页面标题 | `若依管理系统` | `NeoCC 信贷管理系统` |
| `ruoyi-ui/.env.staging` | 页面标题 | `若依管理系统` | `NeoCC 信贷管理系统` |
| `ruoyi-ui/src/settings.js` | 底部版权 | `RuoYi` | `NeoCC` |

---

### P1-3: 启用验证码功能 ✅

**修改时间**: 2026-04-16  
**修改类型**: 后端代码修改  
**影响范围**: Auth服务、Redis、登录流程

#### 修改文件清单

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `auth/pom.xml` | 修改 | 添加easy-captcha和Redis依赖 |
| `auth/src/main/java/.../CaptchaController.java` | 新增 | 验证码生成控制器 |
| `auth/src/main/java/.../RuoyiAdapterController.java` | 修改 | 添加验证码验证逻辑 |
| `auth/src/main/resources/application.yml` | 修改 | 添加Swagger配置 |

#### 修改详情

**1. auth/pom.xml - 添加依赖**
```xml
<!-- Easy Captcha 验证码 -->
<dependency>
    <groupId>com.github.whvcse</groupId>
    <artifactId>easy-captcha</artifactId>
    <version>1.6.2</version>
</dependency>

<!-- Spring Data Redis -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-redis</artifactId>
</dependency>
```

**2. CaptchaController.java - 新增验证码控制器**
```java
@RestController
public class CaptchaController {
    
    @Autowired
    private StringRedisTemplate redisTemplate;
    
    @GetMapping("/captchaImage")
    public Result<Map<String, Object>> captchaImage() {
        // 生成验证码
        SpecCaptcha captcha = new SpecCaptcha(130, 48, 4);
        String code = captcha.text().toLowerCase();
        String uuid = UUID.randomUUID().toString();
        
        // 存储到Redis（5分钟过期）
        redisTemplate.opsForValue().set("captcha:" + uuid, code, 5, TimeUnit.MINUTES);
        
        // 返回Base64图片
        Map<String, Object> result = new HashMap<>();
        result.put("captchaEnabled", true);
        result.put("uuid", uuid);
        result.put("img", captcha.toBase64());
        
        return Result.success(result);
    }
}
```

**3. RuoyiAdapterController.java - 添加验证码验证**
```java
@PostMapping("/login")
public Result<Map<String, Object>> login(@RequestBody Map<String, String> loginRequest) {
    String username = loginRequest.get("username");
    String password = loginRequest.get("password");
    String code = loginRequest.get("code");
    String uuid = loginRequest.get("uuid");
    
    // ✅ 验证验证码
    if (code != null && uuid != null && !code.isEmpty()) {
        String cachedCode = redisTemplate.opsForValue().get("captcha:" + uuid);
        if (cachedCode == null || !cachedCode.equals(code.toLowerCase())) {
            return Result.error(400, "验证码错误");
        }
        // 验证后删除，防止重复使用
        redisTemplate.delete("captcha:" + uuid);
    }
    
    // 调用原有登录逻辑
    SysUserEntity user = sysUserService.login(username, password, "unknown");
    // ...
}
```

#### 验证方法

1. 访问登录页面，应显示验证码图片
2. 输入错误验证码，应提示"验证码错误"
3. 输入正确验证码，应正常登录
4. Redis中验证码5分钟后自动过期

---

### P1-4: 添加XSS防护过滤器 ✅

**修改时间**: 2026-04-16  
**修改类型**: 后端代码修改  
**影响范围**: Gateway服务（所有HTTP请求）

#### 修改文件清单

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `gateway/src/main/java/.../XssUtils.java` | 新增 | XSS清理工具类 |
| `gateway/src/main/java/.../XssFilter.java` | 新增 | XSS防护过滤器 |

#### 修改详情

**1. XssUtils.java - XSS清理工具类**
```java
public class XssUtils {
    
    public static String cleanXss(String value) {
        if (value == null) return null;
        
        // 移除<script>标签
        value = value.replaceAll("(?i)<script[^>]*>(.*?)</script>", "");
        
        // 移除javascript:协议
        value = value.replaceAll("(?i)javascript:", "");
        
        // 移除on*事件处理器
        value = value.replaceAll("(?i)on\\w+\\s*=\\s*\"[^\"]*\"", "");
        
        // 移除危险标签
        value = value.replaceAll("(?i)</?(iframe|object|embed|applet|form)[^>]*>", "");
        
        return value;
    }
    
    public static boolean containsXss(String value) {
        if (value == null) return false;
        
        String lowerValue = value.toLowerCase();
        return lowerValue.contains("<script") ||
               lowerValue.contains("javascript:") ||
               lowerValue.contains("onload") ||
               lowerValue.contains("<iframe");
    }
}
```

**2. XssFilter.java - XSS防护过滤器**
```java
@Component
public class XssFilter implements GlobalFilter, Ordered {
    
    private static final Logger log = LoggerFactory.getLogger(XssFilter.class);
    
    @Override
    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
        ServerHttpRequest request = exchange.getRequest();
        
        // 只处理JSON请求
        List<String> contentTypeHeaders = request.getHeaders().get(HttpHeaders.CONTENT_TYPE);
        if (contentTypeHeaders != null && contentTypeHeaders.stream()
                .anyMatch(ct -> ct.contains(MediaType.APPLICATION_JSON_VALUE))) {
            
            // 包装请求，过滤XSS
            ServerHttpRequestDecorator decoratedRequest = new ServerHttpRequestDecorator(request) {
                @Override
                public Flux<DataBuffer> getBody() {
                    return super.getBody()
                        .map(dataBuffer -> {
                            String body = dataBuffer.toString(StandardCharsets.UTF_8);
                            
                            // 检查是否包含XSS
                            if (XssUtils.containsXss(body)) {
                                log.warn("检测到XSS攻击尝试，IP: {}, URI: {}", 
                                    request.getRemoteAddress(), request.getURI());
                                body = XssUtils.cleanXss(body);
                            }
                            
                            byte[] cleanedBytes = body.getBytes(StandardCharsets.UTF_8);
                            return exchange.getResponse().bufferFactory().wrap(cleanedBytes);
                        });
                }
            };
            
            return chain.filter(exchange.mutate().request(decoratedRequest).build());
        }
        
        return chain.filter(exchange);
    }
    
    @Override
    public int getOrder() {
        return -100; // 在AuthFilter之前执行
    }
}
```

#### 验证方法

1. 发送包含`<script>`标签的请求
2. Gateway日志应显示警告信息
3. 请求体中的危险脚本应被清除
4. 正常业务数据不受影响

---

### P2-1: 添加Swagger接口文档 ✅

**修改时间**: 2026-04-16  
**修改类型**: 后端代码修改  
**影响范围**: 所有微服务模块

#### 修改文件清单

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `auth/pom.xml` | 修改 | 添加springdoc-openapi依赖 |
| `sales/pom.xml` | 修改 | 添加springdoc-openapi依赖 |
| `finance/pom.xml` | 修改 | 添加springdoc-openapi依赖 |
| `system/pom.xml` | 修改 | 添加springdoc-openapi依赖 |
| `auth/src/main/resources/application.yml` | 修改 | 添加Swagger配置 |

#### 修改详情

**1. 各模块pom.xml - 添加依赖**
```xml
<!-- SpringDoc OpenAPI (Swagger) -->
<dependency>
    <groupId>org.springdoc</groupId>
    <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
    <version>2.5.0</version>
</dependency>
```

**2. auth/application.yml - 添加Swagger配置**
```yaml
# Swagger配置
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
  info:
    title: NeoCC 认证服务API
    version: 1.0.0
    description: NeoCC信贷管理系统 - 认证授权模块
```

#### 访问地址

| 服务 | 访问地址 |
|------|----------|
| **Auth服务** | http://localhost:8085/swagger-ui.html |
| **Sales服务** | http://localhost:8083/swagger-ui.html |
| **Finance服务** | http://localhost:8084/swagger-ui.html |
| **System服务** | http://localhost:8082/swagger-ui.html |

#### 验证方法

1. 浏览器访问上述地址
2. 检查是否显示Swagger UI页面
3. 查看API列表是否正常
4. 测试接口调用功能

---

## ⚠️ 待实现的后端修改（已记录）

**优先级**: P1（建议修复）  
**预计工作量**: 2天  
**影响范围**: Auth服务、数据库设计、前端适配

#### 当前问题

**文件**: `auth/src/main/java/com/dafuweng/auth/controller/RuoyiAdapterController.java`

**问题代码**（第105-164行）:
```java
@GetMapping("/getRouters")
public Result<List<Map<String, Object>>> getRouters(@RequestHeader(value = "Authorization", required = false) String token) {
    Long userId = getUserIdFromToken(token);
    
    List<Map<String, Object>> routers = new ArrayList<>();
    
    // ❌ 硬编码菜单
    Map<String, Object> systemMenu = new HashMap<>();
    systemMenu.put("name", "System");
    systemMenu.put("path", "/system");
    // ... 所有菜单都是硬编码
}
```

#### 修复方案

**步骤1: 创建菜单表**

```sql
CREATE TABLE `sys_menu` (
  `menu_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '菜单ID',
  `parent_id` BIGINT DEFAULT 0 COMMENT '父菜单ID',
  `menu_name` VARCHAR(50) NOT NULL COMMENT '菜单名称',
  `menu_type` CHAR(1) NOT NULL COMMENT '菜单类型（M目录 C菜单 F按钮）',
  `path` VARCHAR(200) DEFAULT '' COMMENT '路由路径',
  `component` VARCHAR(255) DEFAULT NULL COMMENT '组件路径',
  `icon` VARCHAR(100) DEFAULT '#' COMMENT '菜单图标',
  `order_num` INT DEFAULT 0 COMMENT '显示顺序',
  `perms` VARCHAR(100) DEFAULT NULL COMMENT '权限标识',
  `visible` CHAR(1) DEFAULT '0' COMMENT '菜单状态（0显示 1隐藏）',
  `status` CHAR(1) DEFAULT '0' COMMENT '菜单状态（0正常 1停用）',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除（0正常 1删除）',
  PRIMARY KEY (`menu_id`),
  KEY `idx_parent_id` (`parent_id`),
  KEY `idx_visible` (`visible`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜单权限表';
```

**步骤2: 初始化菜单数据**

```sql
-- 系统管理菜单
INSERT INTO `sys_menu` VALUES (1, 0, '系统管理', 'M', '/system', 'Layout', 'system', 1, NULL, '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (100, 1, '用户管理', 'C', 'user', 'system/user/index', 'user', 1, 'system:user:list', '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (101, 1, '角色管理', 'C', 'role', 'system/role/index', 'peoples', 2, 'system:role:list', '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (102, 1, '菜单管理', 'C', 'menu', 'system/menu/index', 'tree-table', 3, 'system:menu:list', '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (103, 1, '部门管理', 'C', 'department', 'system/department/index', 'tree', 4, 'system:dept:list', '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (104, 1, '区域管理', 'C', 'zone', 'system/zone/index', 'map', 5, 'system:zone:list', '0', '0', NOW(), NOW(), 0);

-- 销售管理菜单
INSERT INTO `sys_menu` VALUES (2, 0, '销售管理', 'M', '/sales', 'Layout', 'shopping', 2, NULL, '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (200, 2, '客户管理', 'C', 'customer', 'sales/customer/index', 'peoples', 1, 'sales:customer:list', '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (201, 2, '合同管理', 'C', 'contract', 'sales/contract/index', 'edit', 2, 'sales:contract:list', '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (202, 2, '跟进记录', 'C', 'contact-record', 'sales/contact-record/index', 'message', 3, 'sales:contact:list', '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (203, 2, '工作日志', 'C', 'work-log', 'sales/work-log/index', 'log', 4, 'sales:worklog:list', '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (204, 2, '业绩记录', 'C', 'performance-record', 'sales/performance-record/index', 'chart', 5, 'sales:performance:list', '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (205, 2, '客户转移', 'C', 'customer-transfer', 'sales/customer-transfer/index', 'sort', 6, 'sales:transfer:list', '0', '0', NOW(), NOW(), 0);

-- 财务管理菜单
INSERT INTO `sys_menu` VALUES (3, 0, '财务管理', 'M', '/finance', 'Layout', 'money', 3, NULL, '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (300, 3, '贷款审核', 'C', 'loan-audit', 'finance/loan-audit/index', 'audit', 1, 'finance:loan:list', '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (301, 3, '佣金记录', 'C', 'commission', 'finance/commission/index', 'dollar', 2, 'finance:commission:list', '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (302, 3, '服务费记录', 'C', 'service-fee', 'finance/service-fee/index', 'money', 3, 'finance:servicefee:list', '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (303, 3, '银行管理', 'C', 'bank', 'finance/bank/index', 'card', 4, 'finance:bank:list', '0', '0', NOW(), NOW(), 0);
INSERT INTO `sys_menu` VALUES (304, 3, '金融产品', 'C', 'product', 'finance/product/index', 'list', 5, 'finance:product:list', '0', '0', NOW(), NOW(), 0);
```

**步骤3: 创建Menu实体类**

```java
// auth/src/main/java/com/dafuweng/auth/entity/SysMenuEntity.java
package com.dafuweng.auth.entity;

import com.baomidou.mybatisplus.annotation.*;
import lombok.Data;
import java.time.LocalDateTime;

@Data
@TableName("sys_menu")
public class SysMenuEntity {
    @TableId(type = IdType.AUTO)
    private Long menuId;
    private Long parentId;
    private String menuName;
    private String menuType;
    private String path;
    private String component;
    private String icon;
    private Integer orderNum;
    private String perms;
    private String visible;
    private String status;
    
    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createdAt;
    
    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updatedAt;
    
    @TableLogic
    private Integer deleted;
}
```

**步骤4: 创建Menu Mapper和Service**

```java
// auth/src/main/java/com/dafuweng/auth/dao/SysMenuDao.java
package com.dafuweng.auth.dao;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.dafuweng.auth.entity.SysMenuEntity;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface SysMenuDao extends BaseMapper<SysMenuEntity> {
}
```

```java
// auth/src/main/java/com/dafuweng/auth/service/SysMenuService.java
package com.dafuweng.auth.service;

import com.baomidou.mybatisplus.extension.service.IService;
import com.dafuweng.auth.entity.SysMenuEntity;
import java.util.List;
import java.util.Map;

public interface SysMenuService extends IService<SysMenuEntity> {
    /**
     * 根据用户ID获取菜单树
     */
    List<Map<String, Object>> getMenuTreeByUserId(Long userId);
}
```

**步骤5: 修改RuoyiAdapterController**

```java
// auth/src/main/java/com/dafuweng/auth/controller/RuoyiAdapterController.java

@Autowired
private SysMenuService sysMenuService;

@GetMapping("/getRouters")
public Result<List<Map<String, Object>>> getRouters(@RequestHeader(value = "Authorization", required = false) String token) {
    Long userId = getUserIdFromToken(token);
    if (userId == null) {
        return Result.error(401, "未登录");
    }
    
    // ✅ 从数据库动态加载菜单
    List<Map<String, Object>> routers = sysMenuService.getMenuTreeByUserId(userId);
    return Result.success(routers);
}
```

#### 注意事项

1. 需要实现菜单权限过滤（根据用户角色返回对应菜单）
2. 需要处理菜单树形结构构建
3. 前端无需修改（已适配动态菜单）

---

### P1-3: 启用验证码功能

**优先级**: P1（建议修复）  
**预计工作量**: 1天  
**影响范围**: Auth服务、前端登录页

#### 当前问题

**文件**: `auth/src/main/java/com/dafuweng/auth/controller/RuoyiAdapterController.java`

**问题代码**（第24-32行）:
```java
@GetMapping("/captchaImage")
public Result<Map<String, Object>> captchaImage() {
    Map<String, Object> result = new HashMap<>();
    result.put("captchaEnabled", false); // ❌ 关闭验证码
    result.put("uuid", UUID.randomUUID().toString());
    result.put("img", ""); // ❌ 空图片
    return Result.success(result);
}
```

#### 修复方案

**步骤1: 添加验证码依赖**

```xml
<!-- auth/pom.xml -->
<dependency>
    <groupId>com.github.whvcse</groupId>
    <artifactId>easy-captcha</artifactId>
    <version>1.6.2</version>
</dependency>
```

**步骤2: 实现验证码生成**

```java
// auth/src/main/java/com/dafuweng/auth/controller/CaptchaController.java
package com.dafuweng.auth.controller;

import com.dafuweng.common.entity.Result;
import com.wf.captcha.SpecCaptcha;
import org.springframework.data.redis.core.StringRedisTemplate;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.HashMap;
import java.util.Map;
import java.util.UUID;
import java.util.concurrent.TimeUnit;

@RestController
public class CaptchaController {
    
    @Autowired
    private StringRedisTemplate redisTemplate;
    
    @GetMapping("/captchaImage")
    public Result<Map<String, Object>> captchaImage() {
        // 生成验证码
        SpecCaptcha captcha = new SpecCaptcha(130, 48, 4);
        String code = captcha.text().toLowerCase();
        String uuid = UUID.randomUUID().toString();
        
        // 存储到Redis（5分钟过期）
        redisTemplate.opsForValue().set("captcha:" + uuid, code, 5, TimeUnit.MINUTES);
        
        // 返回Base64图片
        Map<String, Object> result = new HashMap<>();
        result.put("captchaEnabled", true);
        result.put("uuid", uuid);
        result.put("img", captcha.toBase64());
        
        return Result.success(result);
    }
}
```

**步骤3: 修改登录逻辑验证验证码**

```java
// auth/src/main/java/com/dafuweng/auth/controller/RuoyiAdapterController.java

@PostMapping("/login")
public Result<Map<String, Object>> login(@RequestBody Map<String, String> loginRequest) {
    String username = loginRequest.get("username");
    String password = loginRequest.get("password");
    String code = loginRequest.get("code");
    String uuid = loginRequest.get("uuid");
    
    // ✅ 验证验证码
    if (code != null && uuid != null) {
        String cachedCode = redisTemplate.opsForValue().get("captcha:" + uuid);
        if (cachedCode == null || !cachedCode.equals(code.toLowerCase())) {
            return Result.error(400, "验证码错误");
        }
        redisTemplate.delete("captcha:" + uuid); // 验证后删除
    }
    
    // 调用原有登录逻辑
    SysUserEntity user = sysUserService.login(username, password, "unknown");
    // ...
}
```

#### 注意事项

1. 需要Redis支持（已配置）
2. 前端登录页已支持验证码显示和输入
3. 可配置验证码开关（生产环境建议启用）

---

### P1-4: 添加XSS防护过滤器

**优先级**: P1（建议修复）  
**预计工作量**: 1天  
**影响范围**: Gateway服务（所有HTTP请求）

#### 当前问题

Gateway未配置XSS防护过滤器，存在跨站脚本攻击风险。

#### 修复方案

**步骤1: 创建XSS过滤器**

```java
// gateway/src/main/java/com/dafuweng/gateway/filter/XssFilter.java
package com.dafuweng.gateway.filter;

import org.springframework.cloud.gateway.filter.GatewayFilterChain;
import org.springframework.cloud.gateway.filter.GlobalFilter;
import org.springframework.core.Ordered;
import org.springframework.core.io.buffer.DataBuffer;
import org.springframework.core.io.buffer.DataBufferUtils;
import org.springframework.http.HttpHeaders;
import org.springframework.http.MediaType;
import org.springframework.http.server.reactive.ServerHttpRequest;
import org.springframework.http.server.reactive.ServerHttpResponse;
import org.springframework.stereotype.Component;
import org.springframework.web.server.ServerWebExchange;
import reactor.core.publisher.Mono;

import java.nio.charset.StandardCharsets;
import java.util.List;

/**
 * XSS防护过滤器
 */
@Component
public class XssFilter implements GlobalFilter, Ordered {
    
    @Override
    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
        ServerHttpRequest request = exchange.getRequest();
        
        // 只处理JSON请求
        List<String> contentTypeHeaders = request.getHeaders().get(HttpHeaders.CONTENT_TYPE);
        if (contentTypeHeaders != null && contentTypeHeaders.stream()
                .anyMatch(ct -> ct.contains(MediaType.APPLICATION_JSON_VALUE))) {
            
            // 包装请求，过滤XSS
            ServerHttpRequest modifiedRequest = new XssServerHttpRequestDecorator(request);
            return chain.filter(exchange.mutate().request(modifiedRequest).build());
        }
        
        return chain.filter(exchange);
    }
    
    @Override
    public int getOrder() {
        return -100; // 在AuthFilter之前执行
    }
}
```

**步骤2: 实现XSS清理逻辑**

```java
// gateway/src/main/java/com/dafuweng/gateway/filter/XssServerHttpRequestDecorator.java
package com.dafuweng.gateway.filter;

import org.springframework.core.io.buffer.DataBuffer;
import org.springframework.core.io.buffer.DefaultDataBufferFactory;
import org.springframework.http.server.reactive.ServerHttpRequest;
import org.springframework.http.server.reactive.ServerHttpRequestDecorator;
import reactor.core.publisher.Flux;

import java.nio.charset.StandardCharsets;

public class XssServerHttpRequestDecorator extends ServerHttpRequestDecorator {
    
    public XssServerHttpRequestDecorator(ServerHttpRequest delegate) {
        super(delegate);
    }
    
    @Override
    public Flux<DataBuffer> getBody() {
        return super.getBody()
            .map(dataBuffer -> {
                String body = dataBuffer.toString(StandardCharsets.UTF_8);
                String cleanedBody = XssUtils.cleanXss(body);
                
                byte[] cleanedBytes = cleanedBody.getBytes(StandardCharsets.UTF_8);
                DataBuffer cleanedBuffer = new DefaultDataBufferFactory().wrap(cleanedBytes);
                return cleanedBuffer;
            });
    }
}
```

**步骤3: XSS清理工具类**

```java
// gateway/src/main/java/com/dafuweng/gateway/filter/XssUtils.java
package com.dafuweng.gateway.filter;

import java.util.regex.Pattern;

/**
 * XSS清理工具类
 */
public class XssUtils {
    
    /**
     * 清理XSS脚本
     */
    public static String cleanXss(String value) {
        if (value == null) {
            return null;
        }
        
        // 移除<script>标签
        value = value.replaceAll("<script>(.*?)</script>", "");
        
        // 移除javascript:协议
        value = value.replaceAll("javascript:", "");
        
        // 移除on*事件处理器
        value = value.replaceAll("on\\w+\\s*=\\s*\"[^\"]*\"", "");
        value = value.replaceAll("on\\w+\\s*=\\s*'[^']*'", "");
        
        // 移除HTML标签（可选，根据业务需求）
        // value = value.replaceAll("<[^>]+>", "");
        
        return value;
    }
}
```

#### 注意事项

1. 需要测试是否影响正常业务数据（如富文本编辑器）
2. 可根据业务需求调整过滤规则
3. 建议配合前端输入验证使用

---

### P2-1: 添加Swagger接口文档

**优先级**: P2（优化项）  
**预计工作量**: 0.5天  
**影响范围**: 所有微服务模块

#### 修复方案

**步骤1: 添加Swagger依赖**

```xml
<!-- 各模块 pom.xml -->
<dependency>
    <groupId>org.springdoc</groupId>
    <artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
    <version>2.5.0</version>
</dependency>
```

**步骤2: 配置Swagger**

```yaml
# 各模块 application.yml
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
  info:
    title: NeoCC API文档
    version: 1.0.0
    description: NeoCC信贷管理系统接口文档
```

**步骤3: 添加注解**

```java
// 示例：CustomerController.java
@RestController
@RequestMapping("/api/sales/customer")
@Tag(name = "客户管理", description = "客户增删改查接口")
public class CustomerController {
    
    @GetMapping("/list")
    @Operation(summary = "查询客户列表", description = "分页查询客户信息")
    public Result<List<Customer>> list(
        @Parameter(description = "页码") @RequestParam(defaultValue = "1") Integer pageNum,
        @Parameter(description = "每页数量") @RequestParam(defaultValue = "10") Integer pageSize
    ) {
        // ...
    }
}
```

#### 访问地址

- Gateway统一访问：`http://localhost:8086/swagger-ui.html`
- 各服务独立访问：
  - Auth: `http://localhost:8085/swagger-ui.html`
  - Sales: `http://localhost:8083/swagger-ui.html`
  - Finance: `http://localhost:8084/swagger-ui.html`
  - System: `http://localhost:8082/swagger-ui.html`

---

### P2-4: 添加操作日志记录

**优先级**: P2（优化项）  
**预计工作量**: 1天  
**影响范围**: 所有微服务模块、数据库

#### 修复方案

**步骤1: 创建操作日志表**

```sql
CREATE TABLE `sys_oper_log` (
  `oper_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '日志主键',
  `title` VARCHAR(50) DEFAULT '' COMMENT '模块标题',
  `business_type` INT DEFAULT 0 COMMENT '业务类型（0其它 1新增 2修改 3删除）',
  `method` VARCHAR(100) DEFAULT '' COMMENT '方法名称',
  `request_method` VARCHAR(10) DEFAULT '' COMMENT '请求方式',
  `operator_type` INT DEFAULT 0 COMMENT '操作类别（0其它 1后台用户 2手机端用户）',
  `oper_name` VARCHAR(50) DEFAULT '' COMMENT '操作人员',
  `dept_name` VARCHAR(50) DEFAULT '' COMMENT '部门名称',
  `oper_url` VARCHAR(255) DEFAULT '' COMMENT '请求URL',
  `oper_ip` VARCHAR(128) DEFAULT '' COMMENT '主机地址',
  `oper_location` VARCHAR(255) DEFAULT '' COMMENT '操作地点',
  `oper_param` VARCHAR(2000) DEFAULT '' COMMENT '请求参数',
  `json_result` VARCHAR(2000) DEFAULT '' COMMENT '返回参数',
  `status` INT DEFAULT 0 COMMENT '操作状态（0正常 1异常）',
  `error_msg` VARCHAR(2000) DEFAULT '' COMMENT '错误消息',
  `oper_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
  `cost_time` BIGINT DEFAULT 0 COMMENT '消耗时间（毫秒）',
  PRIMARY KEY (`oper_id`),
  KEY `idx_oper_name` (`oper_name`),
  KEY `idx_oper_time` (`oper_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志记录表';
```

**步骤2: 创建日志实体和Service**

```java
// common/src/main/java/com/dafuweng/common/entity/SysOperLogEntity.java
package com.dafuweng.common.entity;

import com.baomidou.mybatisplus.annotation.*;
import lombok.Data;
import java.time.LocalDateTime;

@Data
@TableName("sys_oper_log")
public class SysOperLogEntity {
    @TableId(type = IdType.AUTO)
    private Long operId;
    private String title;
    private Integer businessType;
    private String method;
    private String requestMethod;
    private Integer operatorType;
    private String operName;
    private String deptName;
    private String operUrl;
    private String operIp;
    private String operLocation;
    private String operParam;
    private String jsonResult;
    private Integer status;
    private String errorMsg;
    private LocalDateTime operTime;
    private Long costTime;
}
```

**步骤3: 创建日志注解**

```java
// common/src/main/java/com/dafuweng/common/annotation/OperLog.java
package com.dafuweng.common.annotation;

import java.lang.annotation.*;

/**
 * 操作日志注解
 */
@Target({ElementType.METHOD})
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface OperLog {
    /**
     * 模块名称
     */
    String title() default "";
    
    /**
     * 业务类型
     */
    BusinessType businessType() default BusinessType.OTHER;
}

enum BusinessType {
    OTHER(0),      // 其它
    INSERT(1),     // 新增
    UPDATE(2),     // 修改
    DELETE(3),     // 删除
    GRANT(4),      // 授权
    EXPORT(5),     // 导出
    IMPORT(6),     // 导入
    FORCE(7),      // 强退
    GENCODE(8),    // 生成代码
    CLEAN(9);      // 清空数据
    
    private final int value;
    
    BusinessType(int value) {
        this.value = value;
    }
    
    public int value() {
        return this.value;
    }
}
```

**步骤4: 创建日志切面**

```java
// common/src/main/java/com/dafuweng/common/aspect/OperLogAspect.java
package com.dafuweng.common.aspect;

import com.dafuweng.common.annotation.OperLog;
import com.dafuweng.common.entity.SysOperLogEntity;
import com.dafuweng.common.service.SysOperLogService;
import org.aspectj.lang.JoinPoint;
import org.aspectj.lang.annotation.AfterReturning;
import org.aspectj.lang.annotation.AfterThrowing;
import org.aspectj.lang.annotation.Aspect;
import org.aspectj.lang.annotation.Before;
import org.aspectj.lang.reflect.MethodSignature;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import org.springframework.web.context.request.RequestContextHolder;
import org.springframework.web.context.request.ServletRequestAttributes;

import javax.servlet.http.HttpServletRequest;
import java.lang.reflect.Method;
import java.util.Map;

/**
 * 操作日志切面
 */
@Aspect
@Component
public class OperLogAspect {
    
    @Autowired
    private SysOperLogService sysOperLogService;
    
    private ThreadLocal<Long> startTime = new ThreadLocal<>();
    
    @Before("@annotation(operLog)")
    public void doBefore(OperLog operLog) {
        startTime.set(System.currentTimeMillis());
    }
    
    @AfterReturning(pointcut = "@annotation(operLog)", returning = "jsonResult")
    public void doAfterReturning(JoinPoint joinPoint, OperLog operLog, Object jsonResult) {
        handleLog(joinPoint, operLog, null, jsonResult);
    }
    
    @AfterThrowing(pointcut = "@annotation(operLog)", throwing = "e")
    public void doAfterThrowing(JoinPoint joinPoint, OperLog operLog, Exception e) {
        handleLog(joinPoint, operLog, e, null);
    }
    
    protected void handleLog(JoinPoint joinPoint, OperLog operLog, Exception e, Object jsonResult) {
        try {
            // 获取方法签名
            MethodSignature signature = (MethodSignature) joinPoint.getSignature();
            Method method = signature.getMethod();
            
            // 创建日志实体
            SysOperLogEntity operLogEntity = new SysOperLogEntity();
            operLogEntity.setTitle(operLog.title());
            operLogEntity.setBusinessType(operLog.businessType().value());
            operLogEntity.setMethod(method.getDeclaringClass().getName() + "." + method.getName());
            
            // 获取请求信息
            ServletRequestAttributes attributes = (ServletRequestAttributes) RequestContextHolder.getRequestAttributes();
            if (attributes != null) {
                HttpServletRequest request = attributes.getRequest();
                operLogEntity.setRequestMethod(request.getMethod());
                operLogEntity.setOperUrl(request.getRequestURI());
                operLogEntity.setOperIp(request.getRemoteAddr());
            }
            
            // 计算耗时
            operLogEntity.setCostTime(System.currentTimeMillis() - startTime.get());
            
            // 设置状态
            if (e != null) {
                operLogEntity.setStatus(1);
                operLogEntity.setErrorMsg(e.getMessage());
            } else {
                operLogEntity.setStatus(0);
                operLogEntity.setJsonResult(jsonResult != null ? jsonResult.toString().substring(0, Math.min(2000, jsonResult.toString().length())) : "");
            }
            
            // 异步保存日志
            sysOperLogService.saveOperLog(operLogEntity);
            
        } catch (Exception ex) {
            // 日志记录失败不影响业务
            ex.printStackTrace();
        }
    }
}
```

**步骤5: 使用日志注解**

```java
// 示例：CustomerController.java
@RestController
@RequestMapping("/api/sales/customer")
public class CustomerController {
    
    @PostMapping
    @OperLog(title = "客户管理", businessType = BusinessType.INSERT)
    public Result<Void> addCustomer(@RequestBody Customer customer) {
        // ...
    }
    
    @PutMapping
    @OperLog(title = "客户管理", businessType = BusinessType.UPDATE)
    public Result<Void> updateCustomer(@RequestBody Customer customer) {
        // ...
    }
    
    @DeleteMapping("/{id}")
    @OperLog(title = "客户管理", businessType = BusinessType.DELETE)
    public Result<Void> deleteCustomer(@PathVariable Long id) {
        // ...
    }
}
```

---

## 📋 总结

### 已完成修改

- ✅ **P1-1**: 前端页面标题更新为NeoCC（4个文件修改）
- ✅ **P1-3**: 验证码功能启用（4个文件修改/新增）
- ✅ **P1-4**: XSS防护过滤器（2个文件新增）
- ✅ **P2-1**: Swagger接口文档（5个文件修改）

### 待实现修改（已记录）

| 问题 | 预计工作量 | 涉及模块 | 优先级 |
|------|-----------|---------|--------|
| P1-2: 菜单动态化 | 2天 | Auth服务+数据库 | P1 |
| P2-4: 操作日志 | 1天 | 所有服务+数据库 | P2 |

**总计工作量**: 3天

### 修改统计

| 修改类型 | 文件数 | 新增文件 | 修改文件 |
|---------|--------|---------|---------|
| 前端配置 | 4 | 0 | 4 |
| 后端代码 | 6 | 3 | 3 |
| 配置文件 | 5 | 0 | 5 |
| **合计** | **15** | **3** | **12** |

### 下一步建议

1. **立即执行**: 重启系统验证修改效果（详见《系统重启指南.md》）
2. **优先实施**: P1-2菜单动态化（提升系统灵活性）
3. **后续优化**: P2-4操作日志（提升可维护性）

---

**修改日志生成时间**: 2026-04-16  
**下次更新**: 实施待实现修改后更新本文档
