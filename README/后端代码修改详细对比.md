# NeoCC 后端代码修改详细对比

本文档详细列出与 GitHub 仓库原始代码相比，后端所有修改的文件及具体变更内容。

**最后更新**：2026-04-16  
**当前状态**：✅ 所有核心修改已完成并部署

---

## 一、Gateway 模块修改

### 1.1 AuthFilter.java - 认证过滤器（重要）

**文件路径**: `gateway/src/main/java/com/dafuweng/gateway/filter/AuthFilter.java`

#### 修改内容对比

| 行号 | 原始代码 | 修改后代码 | 说明 |
|-----|---------|-----------|------|
| 44-53 | 无 | 新增 `PUBLIC_PATHS` 白名单常量 | RuoYi 前端公开接口列表 |
| 65-78 | 无白名单逻辑 | 新增三段白名单判断 | 放行 `/api/**` 和 `/system/**` |

#### 完整代码差异

```java
// ========== 新增常量 ==========
// 原始：无
// 修改后：
private static final List<String> PUBLIC_PATHS = List.of(
    "/captchaImage",
    "/login",
    "/logout",
    "/register",
    "/getInfo",
    "/getRouters",
    "/unlockscreen"
);

// ========== filter 方法修改 ==========
// 原始：只放行 /auth/api/sysUser/login
if (path.contains("/auth/api/sysUser/login") || path.contains("/auth/api/sysUser/page")) {
    return chain.filter(exchange);
}

// 修改后：添加三段白名单判断
if (path.contains("/auth/api/sysUser/login") || path.contains("/auth/api/sysUser/page")) {
    return chain.filter(exchange);
}

// 跳过 RuoYi 前端公开接口
if (PUBLIC_PATHS.contains(path)) {
    return chain.filter(exchange);
}

// 跳过业务 API 路径（/api/**）
if (path.startsWith("/api/")) {
    return chain.filter(exchange);
}

// 跳过系统管理路径（/system/**）
if (path.startsWith("/system/")) {
    return chain.filter(exchange);
}
```

#### 修改原因
1. **RuoYi 前端需要公开接口**：验证码、登录、获取用户信息等接口不需要认证
2. **业务 API 暂不需要认证**：`/api/customer`、`/api/contract` 等接口放行
3. **系统管理接口返回模拟数据**：`/system/user/list` 等接口放行

---

### 1.2 application.yml - 网关路由配置（重要）

**文件路径**: `gateway/src/main/resources/application.yml`

#### 主要变更

| 配置项 | 原始值 | 修改后 | 影响 |
|-------|-------|-------|------|
| auth-root-route | 无 | 新增 | RuoYi 前端根路径接口 |
| finance-route uri | `lb://finance` | `http://neocc-finance:8084` | 直接 URL（待迁移到 Nacos） |
| sales-route uri | `lb://sales` | `http://neocc-sales:8083` | 直接 URL（待迁移到 Nacos） |
| system-route uri | `lb://system` | `http://neocc-system:8082` | 直接 URL（待迁移到 Nacos） |
| auth-route uri | `lb://auth` | `lb://auth` | 保持 Nacos 服务发现 |
| StripPrefix | 有 | 移除（部分路由） | 保留完整路径 |
| sales-api-* | 无 | 新增 6 个路由 | 销售模块 API |
| finance-api-* | 无 | 新增 5 个路由 | 财务模块 API |
| system-api-* | 无 | 新增 2 个路由 | 系统模块 API |
| auth-api-* | 无 | 新增 3 个路由 | 认证模块 API |
| CORS allowedOriginPatterns | `http://localhost:3000` | `*` | 允许所有来源 |
| CORS allowCredentials | `true` | `false` | 配合 allowedOriginPatterns |

#### 新增路由配置详情

```yaml
# RuoYi 前端适配：登录认证相关接口（根路径）
- id: auth-root-route
  uri: http://neocc-auth:8085
  predicates:
    - Path=/captchaImage,/login,/logout,/register,/getInfo,/getRouters,/unlockscreen

# Auth 服务（使用 Nacos 服务发现）
- id: auth-route
  uri: lb://auth
  predicates:
    - Path=/auth/**
  filters:
    - StripPrefix=1

# 销售模块 API 路由（新增 6 个）
- id: sales-api-customer
  uri: http://neocc-sales:8083
  predicates:
    - Path=/api/customer/**

# ... 其他 /api/** 路由（共 17 个）
```

---

## 二、Auth 模块修改

### 2.1 SecurityConfig.java - 安全配置

**文件路径**: `auth/src/main/java/com/dafuweng/auth/config/SecurityConfig.java`

#### 修改内容

```java
// 新增：禁用 logout 功能（RuoYi 前端自己处理）
.logout(logout -> logout.disable())

// 新增：RuoYi 前端适配接口白名单
.requestMatchers("/captchaImage", "/login", "/logout", "/register", "/getInfo", "/getRouters", "/unlockscreen").permitAll()
```

---

### 2.2 RuoyiAdapterController.java - 新增文件（重要）

**文件路径**: `auth/src/main/java/com/dafuweng/auth/controller/RuoyiAdapterController.java`

**状态**: 新增文件（原仓库不存在）

#### 功能说明

适配 RuoYi-Vue3 前端所需的接口格式，包括：

| 接口 | 路径 | 功能 |
|-----|------|------|
| 验证码 | `GET /captchaImage` | 返回模拟验证码（关闭验证码功能） |
| 登录 | `POST /login` | 适配 RuoYi 登录格式 |
| 获取用户信息 | `GET /getInfo` | 返回用户、角色、权限信息 |
| 获取路由菜单 | `GET /getRouters` | 返回系统管理、销售管理、财务管理菜单 |
| 退出登录 | `POST /logout` | 返回成功 |
| 退出登录 | `GET /login?logout` | 处理 Spring Security 跳转 |
| 注册 | `POST /register` | 返回成功（功能未开放） |
| 解锁屏幕 | `POST /unlockscreen` | 返回成功 |

#### 关键代码片段

```java
/**
 * 登录接口 - 适配 RuoYi 格式
 */
@PostMapping("/login")
public Result<Map<String, Object>> login(@RequestBody Map<String, String> loginRequest) {
    String username = loginRequest.get("username");
    String password = loginRequest.get("password");
    
    SysUserEntity user = sysUserService.login(username, password, "unknown");
    
    // 适配 RuoYi 响应格式
    Map<String, Object> result = new HashMap<>();
    result.put("token", user.getId().toString()); // 使用 userId 作为 token
    result.put("expires_in", 7200);
    
    return Result.success(result);
}

/**
 * 获取路由菜单 - 适配 RuoYi 格式
 */
@GetMapping("/getRouters")
public Result<List<Map<String, Object>>> getRouters(@RequestHeader(value = "Authorization", required = false) String token) {
    List<Map<String, Object>> routers = new ArrayList<>();
    
    // 系统管理菜单
    Map<String, Object> systemMenu = new HashMap<>();
    systemMenu.put("name", "System");
    systemMenu.put("path", "/system");
    systemMenu.put("component", "Layout");
    // ... 子菜单：用户管理、角色管理、菜单管理
    
    // 销售管理菜单
    Map<String, Object> salesMenu = new HashMap<>();
    salesMenu.put("name", "Sales");
    salesMenu.put("path", "/sales");
    // ... 子菜单：客户管理、合同管理
    
    // 财务管理菜单
    Map<String, Object> financeMenu = new HashMap<>();
    financeMenu.put("name", "Finance");
    financeMenu.put("path", "/finance");
    // ... 子菜单：贷款审核、佣金记录
    
    return Result.success(routers);
}
```

---

## 三、Sales 模块修改

### 3.1 application.yml - 应用配置

**文件路径**: `sales/src/main/resources/application.yml`

#### 修改内容

```yaml
# 数据库连接
# 原始：
url: jdbc:mysql://localhost:3306/dafuweng_sales?...

# 修改后：
url: jdbc:mysql://neocc-mysql:3306/dafuweng_sales?...

# Nacos 配置（已启用）
# 原始：
cloud:
  nacos:
    discovery:
      server-addr: localhost:8848
      password: nacos
      username: nacos
      namespace: afae1f3f-8800-48c4-a161-c525a45538c9

# 修改后（保持启用，使用 Docker 网络）：
cloud:
  nacos:
    discovery:
      server-addr: neocc-nacos:8848
```

**说明**：当前版本已启用 Nacos 服务发现，使用 Docker 容器名连接

---

### 3.2 ContractEntity.java - 实体类

**文件路径**: `sales/src/main/java/com/dafuweng/sales/entity/ContractEntity.java`

#### 修改内容

```java
// 新增 @TableField 注解，显式映射带下划线的列名

@TableField("service_fee_1")
private BigDecimal serviceFee1;

@TableField("service_fee_2")
private BigDecimal serviceFee2;

@TableField("service_fee_1_paid")
private Short serviceFee1Paid;

@TableField("service_fee_2_paid")
private Short serviceFee2Paid;

@TableField("service_fee_1_pay_date")
private Date serviceFee1PayDate;

@TableField("service_fee_2_pay_date")
private Date serviceFee2PayDate;
```

#### 修改原因
MyBatis-Plus 默认将驼峰命名转换为下划线命名，但数据库字段已经是下划线命名，需要显式指定。

---

### 3.3 PerformanceRecordDao.xml - MyBatis 映射文件

**文件路径**: `sales/src/main/resources/sales/mapper/PerformanceRecordDao.xml`

#### 修改内容

```xml
<!-- 原始：resultMap id="performanceRecordMap" -->
<!-- 修改后：resultMap id="BaseResultMap" -->
<resultMap id="BaseResultMap" type="com.dafuweng.sales.entity.PerformanceRecordEntity">
    ...
</resultMap>

<!-- 原始：resultMap="performanceRecordMap" -->
<!-- 修改后：resultMap="BaseResultMap" -->
<select id="selectByContractId" resultMap="BaseResultMap">
```

#### 修改原因
MyBatis-Plus 默认使用 `BaseResultMap` 作为结果映射名称。

---

### 3.4 RuoyiSalesController.java - 新增文件

**文件路径**: `sales/src/main/java/com/dafuweng/sales/controller/RuoyiSalesController.java`

**状态**: 新增文件

#### 功能说明
适配 RuoYi 前端所需的销售模块接口，返回模拟数据。

---

## 四、Finance 模块修改

### 4.1 application.yml - 应用配置

**文件路径**: `finance/src/main/resources/application.yml`

#### 修改内容（同 Sales 模块）

```yaml
# 数据库连接
url: jdbc:mysql://neocc-mysql:3306/dafuweng_finance?...

# Nacos 配置（已启用）
cloud:
  nacos:
    discovery:
      server-addr: neocc-nacos:8848
```

**说明**：当前版本已启用 Nacos 服务发现，使用 Docker 容器名连接

---

### 4.2 RuoyiFinanceController.java - 新增文件

**文件路径**: `finance/src/main/java/com/dafuweng/finance/controller/RuoyiFinanceController.java`

**状态**: 新增文件

#### 功能说明
适配 RuoYi 前端所需的财务模块接口，返回模拟数据。

---

## 五、System 模块新增文件

### 5.1 RuoyiSystemController.java - 新增文件

**文件路径**: `system/src/main/java/com/dafuweng/system/controller/RuoyiSystemController.java`

**状态**: 新增文件

#### 功能说明
适配 RuoYi 前端所需的系统管理模块接口，包括：
- 用户管理（CRUD、重置密码、修改状态）
- 角色管理（CRUD、数据权限、分配用户）
- 菜单管理（CRUD、树形选择）
- 部门管理（CRUD）
- 岗位管理（CRUD）
- 参数管理（CRUD）
- 通知公告（CRUD）

所有接口返回模拟数据（空列表或空对象）。

---

### 5.2 RuoyiMonitorController.java - 新增文件

**文件路径**: `system/src/main/java/com/dafuweng/system/controller/RuoyiMonitorController.java`

**状态**: 新增文件

#### 功能说明
适配 RuoYi 前端所需的监控模块接口，返回模拟数据。

---

## 六、修改统计

### 6.1 修改的文件列表

| 模块 | 文件 | 修改类型 |
|-----|------|---------|
| gateway | AuthFilter.java | 修改 |
| gateway | application.yml | 修改 |
| auth | SecurityConfig.java | 修改 |
| auth | RuoyiAdapterController.java | 新增 |
| sales | application.yml | 修改 |
| sales | ContractEntity.java | 修改 |
| sales | PerformanceRecordDao.xml | 修改 |
| sales | RuoyiSalesController.java | 新增 |
| finance | application.yml | 修改 |
| finance | RuoyiFinanceController.java | 新增 |
| system | RuoyiSystemController.java | 新增 |
| system | RuoyiMonitorController.java | 新增 |

### 6.2 关键修改点总结

1. **Gateway 路由**: Auth 服务使用 `lb://` + Nacos，其他服务暂时使用 `http://` + Docker 容器名
2. **Gateway 认证**: 添加 `/api/**` 和 `/system/**` 白名单
3. **Auth 适配**: 新增 RuoYi 前端所需的所有根路径接口
4. **微服务配置**: 启用 Nacos，使用 Docker 网络主机名连接数据库和注册中心
5. **MyBatis 映射**: 添加 `@TableField` 注解，修复 resultMap 名称
6. **适配控制器**: 新增 5 个适配控制器，处理 RuoYi 标准功能
7. **前端页面**: 新增 16 个业务页面，完全对接后端 API

---

## 七、建议同步给后端团队的配置

### 7.1 必须同步的修改

1. **Gateway application.yml**: 路由配置包含 17 个 /api/** 路由 + Nacos 服务发现
2. **AuthFilter.java**: 白名单逻辑是前端正常工作的关键
3. **RuoyiAdapterController.java**: RuoYi 前端依赖这些接口
4. **微服务 application.yml**: Nacos 和数据库连接配置（使用 Docker 容器名）

### 7.2 可选同步的修改

1. **MyBatis 映射**: 如果后端使用相同的实体类
2. **适配控制器**: 如果后端需要支持 RuoYi 前端

### 7.3 数据库表结构修复

以下数据库修改也需要同步：

```sql
-- Customer 表
ALTER TABLE customer 
  MODIFY phone varchar(20) NULL,
  MODIFY created_by bigint NULL,
  MODIFY dept_id bigint NULL,
  MODIFY sales_rep_id bigint NULL,
  MODIFY zone_id bigint NULL;

-- Contract 表
ALTER TABLE contract ADD COLUMN zone_id bigint NULL AFTER product_id;
ALTER TABLE contract 
  MODIFY contract_amount decimal(15,2) NULL,
  MODIFY created_by bigint NULL,
  MODIFY dept_id bigint NULL,
  MODIFY sales_rep_id bigint NULL,
  MODIFY service_fee_1 decimal(15,2) DEFAULT 0,
  MODIFY service_fee_2 decimal(15,2) DEFAULT 0,
  MODIFY service_fee_rate decimal(5,4) DEFAULT 0;
```

---

## 八、前后端对接清单

### 8.1 已完成对接的模块（15 个）

| 模块 | 后端 Controller | 前端页面 | API 文件 | 状态 |
|------|----------------|---------|---------|------|
| 客户管理 | CustomerController | sales/customer/index.vue | api/sales/customer.js | ✅ |
| 合同管理 | ContractController | sales/contract/index.vue | api/sales/contract.js | ✅ |
| 贷款审核 | LoanAuditController | finance/loan-audit/index.vue | api/finance/loanAudit.js | ✅ |
| 佣金记录 | CommissionRecordController | finance/commission/index.vue | api/finance/commission.js | ✅ |
| 服务费记录 | ServiceFeeRecordController | finance/service-fee/index.vue | api/finance/serviceFeeRecord.js | ✅ |
| 业绩记录 | PerformanceRecordController | sales/performance-record/index.vue | api/sales/performanceRecord.js | ✅ |
| 跟进记录 | ContactRecordController | sales/contact-record/index.vue | api/sales/contactRecord.js | ✅ |
| 工作日志 | WorkLogController | sales/work-log/index.vue | api/sales/workLog.js | ✅ |
| 客户转移 | CustomerTransferLogController | sales/customer-transfer/index.vue | api/sales/customerTransfer.js | ✅ |
| 银行管理 | BankController | finance/bank/index.vue | api/finance/bank.js | ✅ |
| 金融产品 | FinanceProductController | finance/product/index.vue | api/finance/financeProduct.js | ✅ |
| 部门管理 | SysDepartmentController | system/department/index.vue | api/system/department.js | ✅ |
| 区域管理 | SysZoneController | system/zone/index.vue | api/system/zone.js | ✅ |
| 用户管理 | SysUserController | system/user/index.vue | api/system/user.js | ✅ |
| 角色管理 | SysRoleController | system/role/index.vue | api/system/role.js | ✅ |

### 8.2 部分对接的模块（1 个）

| 模块 | 后端 | 前端 | 说明 |
|------|------|------|------|
| 菜单管理 | 适配层（模拟数据） | system/menu/index.vue | 待整合权限管理 |

### 8.3 适配控制器（5 个）

| 控制器 | 模块 | 功能 | 状态 |
|--------|------|------|------|
| RuoyiAdapterController | auth | 登录、获取用户信息、路由菜单 | ✅ |
| RuoyiSystemController | system | 用户、角色、菜单、部门等管理 | ✅ |
| RuoyiMonitorController | system | 在线用户、定时任务、日志等 | ✅ |
| RuoyiSalesController | sales | 销售模块 RuoYi 风格路由 | ✅ |
| RuoyiFinanceController | finance | 财务模块 RuoYi 风格路由 | ✅ |
